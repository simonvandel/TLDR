actor main := {
  receive arguments:args := {

    let x:int := 5;
    let y:int := x;

    let z:int := y * x;

    if(z >= 25 OR z < 5){
      print("Expected");
    }
    else {
      print("Not expected");
    }
  };
};

// z is expected to be 25