actor main := {
  receive arguments:args := {
    var iStep:iterationStep := (step := 10;);
    iStep.step := 5;
    print("can print!");
    printint(iStep.step);
    die;
  };
};

struct iterationStep := {
  step:int;
};

