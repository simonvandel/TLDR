actor main := {
  receive arguments:args := {
    let xs:[int] := [10 .. 20];
    let head:int := xs.[0];
    if(head = 10) {
      print("Expected");
    }
    else {
      print("Not expected");
    };
    die;
  };
};
