actor main := {
  receive arguments:args := {
    let x:test := spawn test;
    for x in [0 .. 100] {
    send test x;
    };
    die;
  };
};

actor test := {
  receive msg:int := {
    print("ja");
    die;
 };
  receive msg2:real := {
    print("nej");
    die;
 };
};
