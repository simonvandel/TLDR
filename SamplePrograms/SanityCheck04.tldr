actor main := {
  receive arguments:args := {
    let res:real := 3 # (5 ^ 3);
    printreal(res);
    if( res = 5.0){
      print("Expected");
    }
    else{
      print("Not expected");
    };
    die;
  }
}

// root should cancel out power
