actor main := {
  receive arguments:args := {
    let res:real := 3.0 # (5.0 ^ 3.0);
    printreal(res);
    if( res = 5.0){
      print("Expected");
    }
    else{
      print("Not expected");
    };
    die;
  }
}

// root should cancel out power
