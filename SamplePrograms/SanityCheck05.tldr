actor main := {
  receive arguments:args := {
    let x:int := 2;
    if (x < 0) {
      print("x is greater than 0");
    } else {
      print("hej");
    };
    die;
  };
};

// prints "can print!"
